import React from 'react'

const Checkbox = props => {
  return (
    <input
      type="checkbox"
      // onClick={props.handleCheck}
      // name={props.name}
      // value={props.value}
    />
  )
}

export default Checkbox
